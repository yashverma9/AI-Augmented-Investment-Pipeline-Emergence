# {{ name }}
{{ tagline }}

**Call: {{ call }}**
{% if status != "complete" %}
> ⚠️ Incomplete analysis — {{ error or "LLM scoring unavailable" }}. Scores below are placeholders.
{% endif %}

## What they do
{{ description }}

## Score: {{ "%.1f"|format(overall_score) }}/100
- Traction: {{ "%.0f"|format(scores.traction) }}/100
- Product specificity: {{ "%.0f"|format(scores.product_specificity) }}/100 — {{ scores_detail.product_specificity.basis }}
- Differentiation: {{ "%.0f"|format(scores.differentiation) }}/100 — {{ scores_detail.differentiation.basis }}
- Market fit: {{ "%.0f"|format(scores.market_fit) }}/100 — {{ scores_detail.market_fit.basis }}

## What would change my mind
{% for point in would_change_mind %}
- {{ point }}
{% endfor %}

## Details
Founders: {{ makers }}
PH votes: {{ votes_count }}
Launch date: {{ launch_date }}

## Links
Website: {{ website }}
PH launch: {{ ph_launch_url }}
{% for link in other_links %}
{{ link.type }}: {{ link.url }}
{% endfor %}

